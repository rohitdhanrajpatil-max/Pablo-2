## 2025-05-14 - [Hospitality Form Optimization]
**Learning:** For hospitality-related forms in this app, using `autoComplete="organization"` for hotel names and `autoComplete="tel"` for mobile contact fields significantly optimizes the mobile user experience.
**Action:** Always include relevant `autoComplete` attributes and ensure `htmlFor`/`id` linking in all form components.
